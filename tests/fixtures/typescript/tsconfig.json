{
  "compilerOptions": {
    "strict": true,
    "noEmit": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    // `@/` is this directory: jobs.ts reaches the store through it.
    "paths": { "@/*": ["./*"] },
  },
}
